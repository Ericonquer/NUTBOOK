# 第一个有效 H1 才是标题

这份语料包含多个 H1，标题契约规定只取正文顶层第一个有效 H1 作为封面标题。

# 第二个 H1 不应被使用

多 H1 文档在编辑器中是合法状态，但封面与宿主标题只能显示第一个。修改标题保存后，应更新的是第一个 H1，而不是顺带改写第二个。

## 预期

- 封面标题 = 「第一个有效 H1 才是标题」；
- 第二个 H1 保持原样，不被当作标题来源；
- 保存、关闭、重开后第一个 H1 的修改持久化，第二个 H1 不受影响。
