# 普通 H1 文档

这是一份带普通一级标题的 Markdown 验收语料，用于验证默认封面取第一个有效 H1。

## 背景

`markdown-default-cover.md` 是最基础的标题形态：正文第一行就是 ATX H1，封面标题应与「# 普通 H1 文档」完全一致。

## 预期

- 默认封面显示正文第一个 H1；
- 标题解析跳过 frontmatter、代码块等非正文区域；
- 修改标题保存后，卡片封面随之刷新。
